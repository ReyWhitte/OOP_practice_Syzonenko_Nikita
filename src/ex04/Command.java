package ex04;

/**
 * Інтерфейс команди (шаблон Command).
 * <p>
 * Визначає базовий метод для виконання команди.
 * </p>
 * 
 * @author Сизоненко Нікіта
 * @version 1.0
 */
public interface Command {
    
    /**
     * Виконує команду.
     */
    void execute();
    
    /**
     * Скасовує виконання команди (для команд, що підтримують undo).
     */
    default void undo() {
        // За замовчуванням - нічого не робимо
    }
}